/// <reference types="node" />
export declare class ElasticBuffer {
    private offset;
    private buffer?;
    private size;
    clear(): void;
    get empty(): boolean;
    reserve(size: number): Buffer;
    getBuffer(size: number): Buffer;
    consume(): Buffer | undefined;
    offer(buffer: Buffer): void;
}
