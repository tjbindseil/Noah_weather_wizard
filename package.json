{
    "name": "weather_wizard",
    "version": "1.0.0",
    "description": "a tool to determine where to activitize",
    "main": "build/index.js",
    "scripts": {
        "run": "npm run tsc && node build/index.js",
        "tsc": "tsc",
        "clean": "rm -rf node_modules package-lock.json build",
        "format": "npx prettier --write $(find src test package*)",
        "lint": "npx eslint $(find src test package*)",
        "build": "tsc && npm run test && npm run format && npm run lint && echo success!",
        "build-reinstall": "npm run clean && npm install && tsc && npm run test && npm run format && npm run lint && echo success!"
    },
    "author": "TJB",
    "license": "ISC",
    "dependencies": {},
    "devDependencies": {
        "eslint-config-prettier": "^8.8.0",
        "@typescript-eslint/eslint-plugin": "^7.3.1",
        "eslint": "^8.40.0",
        "prettier": "2.8.8"
    }
}
